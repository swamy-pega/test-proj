{
  "name": "myquiz-heroku",
  "version": "1.1.0",
  "scripts": {
    "build": "echo 'root build ddskipped for heroku app dddddherokuddddd typdes rrr testdddddd' ",
    "heroku-postbuild": "echo 'Contents:' && ls -la && cd myquiz-app-backup && echo 'Frontend contents:' && ls -la && npm install --legacy-peer-deps && npm run build "
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.6.0",
   "vite": "^6.3.5"
  }
}
