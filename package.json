{
  "name": "myquiz-heroku",
  "version": "1.1.0",
  "scripts": {

 "heroku-postbuild": "echo '📁 CWD BEFORE:' && pwd && cd myquiz-frontend && echo '📁 CWD AFTER:' && pwd && npm install && npm run build",


    "build": "echo 'root build skipped for heroku app heroku '"
  }
}
  