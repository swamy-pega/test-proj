{
  "name": "myquiz-heroku",
  "version": "1.0.0",
  "scripts": {
  
    "heroku-postbuild": "cd myquiz-frontend && npm install && npm run build",
    "build": "echo 'root build skipped for heroku'"
  }
}
