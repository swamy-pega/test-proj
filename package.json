{
  "name": "myquiz-heroku",
  "version": "1.1.0",
  "scripts": {
    "build": "echo 'root build skipped for heroku app heroku '",
   "heroku-postbuild": "echo 'Contents:' && ls -la && cd myquiz-frontend && echo 'Frontend contents:' && ls -la && npm install && npm run build "

  }
}
  