{
  "name": "myquiz-heroku",
  "version": "1.1.0",
  "scripts": {
  
  "heroku-postbuild": "echo '📦 Starting postbuild...' && cd myquiz-frontend && npm install && echo '📦 Building frontend...' && npm run build",

    "build": "echo 'root build skipped for heroku app heroku'"
  }
}
  